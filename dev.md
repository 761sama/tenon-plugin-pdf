# 项目开发规范（dev.md）

> **本文件是项目的「宪法」，面向 AI 与开发者。**
> 每次新会话开始，AI **必须先读取本文件**，再开始任何工作。
> 面向人类的项目介绍与项目配置见 `README.md`（首次初始化时生成）。
> **本文件为只读文件：AI 不得修改、追加或删除本文件中的任何内容。**

---

## 1. 初始化流程（首次会话必读）

**初始化依据：仓库中不存在 `README.md` → 项目尚未初始化。** 此时 AI 必须按顺序执行：

1. **一次性**向 761 提出以下设定问题（不要分多次打扰）：
   - 项目类型（CLI 工具 / Web 服务 / 库 / 桌面应用 / 其他）
   - 项目背景与定位（一句话说明）
   - 主要语言及版本、构建 / 包管理工具
   - 测试框架与测试目录约定
   - 代码注释语言（默认中文）
   - 依赖政策（是否允许引入第三方依赖、是否要求零依赖）
   - 是否有其他特殊约定（命名风格、依赖策略、许可证等）
2. **建立代码仓库**：若项目尚未纳入 git 版本控制（无 `.git`），执行 `git init` 建立本地仓库。（此项属初始化动作，仅此一次——后续会话不再检查仓库是否存在。）
3. 创建 `.gitignore` 和 `doc/` 目录（若尚不存在）。
4. **生成 `README.md`**：按第 5 节模板填写，其中「项目信息」一节必须如实填入上一步的全部回答。
5. **完成首次提交**：按第 2.4 节约定提交初始化骨架——`[init] 初始化项目`，正文逐条列出新增的 `README.md`、`.gitignore`、`doc/` 等。
6. **此后所有会话不再询问这些设定**：项目配置一律以 `README.md` 为准，AI 每次工作按其执行。

---

## 2. 硬性规范（任何会话都必须遵守，不得违反）

### 2.1 目录与文档

- 所有相关文档（设计说明、接口文档、使用说明、排错记录等）一律放在 **`doc/`** 目录下，按主题拆分文件；根目录只保留 `README.md`、`dev.md`、`SUMMARY.md`、`.gitignore` 等必备文件。
- **必须存在 `.gitignore`**：覆盖构建产物、依赖目录、临时文件、日志、编辑器配置等；密钥与凭据**绝不入库**，一律走环境变量或密钥管理。
- `README.md` 只写项目介绍与项目配置（背景、用途、类型、语言版本、构建工具等），开发细节一律进 `doc/`。
- **文档与代码同步**：改动影响到接口、配置项、运行时行为或使用方式时，必须在同一次任务内更新对应的 `doc/` 文档与 `README.md`，不允许留下与代码不一致的过期描述。

### 2.2 代码空行规范

- **空行最多 1 行**：只出现在模块 / 函数 / 逻辑阶段之间，禁止连续 2 行及以上空行。
- **相同功能的代码、同一部分同一步骤的连续代码行之间：不加空行**，紧凑排列。
- 判断标准：按「语义分组」——**组与组之间空 1 行，组内不空行**。

```python
# 正确示例
def process(data: list) -> list:
    result = []

    for item in data:
        result.append(transform(item))  # 同一步骤内，紧凑

    return result
```

### 2.3 函数注释

- **所有函数 / 方法必须有注释**，说明三要素：**用途、入参、出参**。
- 语言支持 docstring 时用 docstring（Python 用 `"""..."""`，JS/TS 用 JSDoc，Rust 用 `///` 等）；否则用块注释。
- 复杂逻辑处可补充必要的行内注释，解释「为什么」而非「是什么」。

```python
def fetch_user(user_id: int) -> dict | None:
    """获取用户信息。

    入参:
        user_id: 用户 ID
    出参:
        dict: 用户信息字典；用户不存在时返回 None
    """
```

### 2.4 命名与提交

- 命名遵循语言社区惯例，清晰优先于简短。
- 提交信息格式**强制统一**（所有项目一律照此执行，不得变体）：
  - **第一行**：`[类型] 简要说明`
    - 类型必须是以下之一：`feat`（新增功能）/ `fix`（修复缺陷）/ `doc`（文档）/ `refactor`（重构）/ `test`（测试）/ `perf`（性能）/ `style`（格式）/ `chore`（构建、依赖、杂项）/ `init`（项目初始化——仅用于首次提交）
    - 简要说明用中文，一句话概括本次变更做了什么，不带句号
  - **第二行起**：空一行后，逐行列出具体内容
    - 一行一个条目，必须以动词开头：`增加了` / `修复了` / `移除了` / `修改了` / `调整了` / `优化了` 等
    - 每条写明涉及的功能 / 逻辑 / 模块

示例：

```text
[feat] 增加 CSV 批量导入

增加了 CSV 解析模块，支持表头自动识别
增加了导入失败时的逐行错误提示
修改了导入入口的参数校验逻辑
```

- 一次提交只做一件事；同一件事的多处改动合入同一次提交，正文逐条列出。
- **提交时机与分支**：AI 在每个独立改动完成时按粒度本地提交（保证进度可回滚，长时间任务尤其必要），直接在当前分支上进行；**不 push**——push 由 761 在确认 `SUMMARY.md` 后执行。

### 2.5 模块化与依赖（高内聚低耦合）

- 代码保持**高内聚、低耦合**：模块职责单一，接口清晰，依赖单向流动（核心不依赖外围，外围依赖核心）。
- 系统采用**最小核心 + 可插拔模块**结构：核心路径保持精简；任一非核心模块被移除 / 禁用后，系统其余部分仍可正常运行（**优雅降级**），而非整体崩溃或需要修改核心代码。
- 模块必须可**独立开发、独立测试、独立替换**；新增功能以新增模块 / 扩展点实现，不改动核心。
- 注意：若某模块被移除后系统功能完全不变，说明该模块未提供实际价值，应质疑其存在意义——「可拆卸」不等于「可有可无」，模块价值在于被核心以明确接口依赖。

### 2.6 安全检查

- 密钥与凭据**绝不进 git**：`.env` 及任何 `.env` 备份/历史文件（`.history/`、`*.env.bak` 等）、含 key 的日志/截图/测试脚本/临时文件、硬编码在代码/注释/文档中的凭据，一律禁止提交。
- 提交前必查：`git status` / `git ls-files`；`.gitignore` 必须排除 `.env*`、`.history/` 等模式。
- 发现误提交（无论是否已 push）：立即 `git reset` 重写提交、将敏感文件移出仓库目录、必要时轮换密钥。
- 本分类为强制检查项。

### 2.7 完成标准与验证

- **任务完成的定义**：改动已实际构建 / 测试 / 运行通过。只写完代码、只做静态检查，都不算完成。
- 必须在真实环境中执行验证（编译、跑测试、启动服务、走一遍主要路径），并记录**实际命令与关键输出**。
- **禁止用描述代替结果**：不得以「应该可以」「预计正常」「理论上支持」作为验证结论；没跑通就如实说明没跑通，并写清卡在哪一步。
- 无法验证的项（缺凭据、缺环境、依赖外部服务）必须在「遗留问题」中写明原因与后续验证方式，不得略过。

### 2.8 任务总结（SUMMARY.md）

- **每次任务结束后必须写入**项目根目录的 `SUMMARY.md`（含中途被中断的任务），写入方式按「是否属于同一功能 / 同一模块」判断：
  - **同一功能 / 同一模块**（本次只是对 `SUMMARY.md` 中已有内容的修改或补充）→ **更新**：在原内容上修订、补充，保留该功能此前的条目，使其反映最新状态。
  - **新功能 / 不同功能**（本次任务与 `SUMMARY.md` 中记录的内容无关）→ **覆盖**：直接重写，不保留旧任务的内容。
- **固定小节顺序**（无内容的写「无」，小节不得省略）：
  1. **任务目标**：本次要做什么、范围边界（做什么、不做什么）
  2. **改动清单**：新增 / 修改 / 删除的文件与模块
  3. **验证结果**：实际执行的命令与关键输出、结论
  4. **遗留问题**：未完成项、已知缺陷、需要 761 决策的事项
- 内容须如实，**不得夸大完成度**：宁可写「未完成」「无法实现」「报错了」，也**绝不谎报**——如实报告的问题可以当场处理，谎报的问题只会在更晚的环节以更高代价暴露。
- `SUMMARY.md` **不得提交进 git**：必须写入 `.gitignore`；提交前确认其未被跟踪，若已被跟踪则 `git rm --cached SUMMARY.md`。

---

## 3. 会话工作流

每次新会话，AI 按以下顺序执行：

1. 读取本文件（dev.md），先明确规范要求。
2. 若仓库中不存在 `README.md` → 执行第 1 节初始化流程。
3. **立即读取 `README.md`**：先了解项目是什么（定位、技术栈、构建与测试方式、依赖政策、特殊约定），再按其记录的配置开展工作。
4. 遵循第 2 节硬性规范开始工作。
5. 任务收尾时按第 2.7 节完成真实验证（必须实际执行，不得只做静态描述）。
6. 按第 2.8 节写入 `SUMMARY.md`（同一功能 / 同一模块则更新，新功能则覆盖），并确认该文件未被 git 跟踪。
7. 本文件为只读：若发现规范缺失、表述不清或与实际情况冲突，**不得自行修改**；dev.md 未覆盖的事项按行业惯例自行决策并继续推进（不得因此阻塞），同时在 `SUMMARY.md` 中显式标注「规范未覆盖，按 X 处理」；规范本身的修订由 761 决定。

---

## 4. 文档职责分工

| 文件 | 职责 | 维护者 |
|------|------|--------|
| `README.md` | 项目介绍 + 项目配置（背景、定位、类型、语言版本、构建工具、测试框架、注释语言、特殊约定） | AI 首次生成，随项目演化更新 |
| `dev.md` | 开发规范与 AI 工作流（本文件） | 761（**只读文件，AI 不得修改**） |
| `doc/` | 各类详细文档，按主题拆分 | AI 随开发过程补充 |

---

## 5. README.md 生成模板

首次初始化时，AI 按以下结构生成 `README.md`（内容用中文，空缺字段如实填写）：

````markdown
# <项目名称>

> <一句话项目定位>

## 简介

<项目背景、解决什么问题>

## 项目信息

| 项目 | 内容 |
|------|------|
| 项目类型 | <CLI 工具 / Web 服务 / 库 / 桌面应用 / 其他> |
| 项目定位 | <一句话说明> |
| 主要语言 / 版本 | <如 Go 1.23> |
| 构建 / 包管理工具 | <如 go build / npm> |
| 测试框架 | <如 go test / vitest> |
| 测试目录约定 | <如 测试与源码同目录> |
| 代码注释语言 | 中文 |
| 提交信息规范 | 统一固定格式，见 dev.md 第 2.4 节 |
| 特殊约定 | <命名风格、依赖政策（可否引入新依赖）、许可证等；无则填「无」> |

## 功能特性

- <特性 1>
- <特性 2>

## 快速开始

### 环境要求

- <语言/运行时版本>
- <依赖项>

### 安装与运行

```bash
<安装命令>
<运行命令>
```

## 项目结构

<简要说明目录结构，至少包含 doc/ 与 dev.md 的指向>

## 文档

- 开发规范与 AI 工作流：见 [dev.md](dev.md)
- 详细文档：见 [doc/](doc/)（按主题拆分）

## 许可证

<如有>
````
