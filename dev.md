# tenon-plugin-pdf — 开发规范

Go 语言 PDF 生成库。模块路径 `gopkg.761sama.com/tenon-plugin-pdf`。

## 目录结构
- `doc/` — 文档
- `build/` — 构建产物
- `data/` — 运行时数据（如需）

## 提交规范
- git 提交信息一律中文
- 每个功能模块完成即提交，保持可追溯
- 提交采用 "[feat/fix/doc/...] 简要说明<换行>具体内容" 作为提交
- 具体内容一行一个, 增加功能 "+ 增加了 xxx 功能/逻辑/模块...", 减少功能 "- 移除了 xxx 功能/逻辑/模块...", 修复功能 "! 修复了 xxx 功能/逻辑/模块...", 修改功能 "~ 修改了 xxx 功能/逻辑/模块..."

## 安全检查
- 密钥与凭据绝不进 git：禁止提交 `.env` 及任何备份/历史、硬编码凭据
- 提交前必查 `git status` / `git ls-files`；`.gitignore` 排除 `.env*`
