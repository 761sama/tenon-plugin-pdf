# TODO — 未完成事项

摘自 `SUMMARY.md` 的「已知未覆盖的部分」，按模块整理，作为后续开发清单。

## 表格（table 包）

- [ ] 合并仅支持矩形区域（ColSpan×RowSpan），超出列数/行数自动截断
- [x] ~~表头行本身不支持合并~~（已支持：HeaderRows 内 Span 用法与数据行一致，
      跨行不越过表头区，超出自动截断）
- [x] ~~超高行/跨行块（超过整页）直接溢出绘制不拆分~~（已支持：超过整页可用高度时
      窗口化拆分续页，切点吸附到不切断任何文本行的边界，找不到干净切点时硬切+裁剪兜底）
  - 剩余边界：表头块本身超过整页时仍溢出绘制；无干净切点的极端情况
    可能裁掉被切断文本行的少量像素（文本提取不丢失）
- [ ] 自适应列宽按比例压缩时不低于最长单词宽，极端窄表仍可能溢出

## 字体

- [ ] 仅支持 glyf 轮廓的 TrueType 子集嵌入（TTF 及 TTC 集合成员）
- [ ] OTF/CFF（CIDFontType0）未实现
- [ ] 变量字体实例化未实现（需先用 fonttools 等工具静态化）
- [ ] 竖排（Identity-V）未实现
- [ ] OpenType 特性仅支持 GSUB liga/rlig 连字
- [ ] 字距仅支持 kern 表，GPOS 型字距未实现
- [ ] 复杂文字 shaping（阿拉伯/印度语系的 ccmp 重排与上下文变形）超出范围
- [ ] Symbol/ZapfDingbats 未内置宽度表，本地排版按 600/1000 估算
      （查看器使用内置度量，渲染不受影响）

## 加密

- [ ] 不支持 RC4 内容加密（V1–V3，纯遗留）
- [ ] PubSec 仅支持 RSA 收件人证书
- [ ] 加密文档不支持增量多重签名（追加修订段需持文件密钥加密）

## 数字签名

- [x] ~~签名必须由本库生成的文档发起~~（已支持：`sign.SignExisting` /
      `AppendSignatureField` 可对任意既有 PDF 增量追加签名字段）
  - 布局支持：经典 xref 表与 xref 流/对象流（ObjStm）均可，含两者混排的
    多段修订链与 /XRefStm 混合引用段；追加的修订段一律写经典 xref 表
  - 剩余边界：加密文档不支持增量追加签名字段（遇加密输入给出明确中文错误）
- [ ] LTV 仅覆盖 DSS 证书/CRL/OCSP 嵌入；CRL/OCSP 的在线获取与签署后增量追加由调用方负责
- [ ] 可见签名外观文本限 WinAnsi 字符（非 ASCII 显示为 `?`）
- [ ] 签名占位空间固定 16KB（超大证书链需调库常量）

## 合规性与高级图形

- [ ] PDF/A、PDF/X、PDF/UA 合规性
- [ ] 标记 PDF（Tagged PDF）
- [ ] 函数类型 0/3/4（采样/拼接/PostScript）
- [ ] 网格渐变（ShadingType 4–7）
- [ ] 平铺/图案 Pattern
- [ ] 可选内容组（图层 OCG）
- [ ] 透明度组 XObject

## 表单与批注

- [ ] 表单：单选按钮、下拉/列表框、JavaScript 动作（签名字段已支持）
- [ ] 其他批注：FreeText、Stamp、Ink、FileAttachment、3D/多媒体

## 文件结构

- [ ] 对象流（ObjStm）/ xref 流的**生成**未实现（读取侧已支持，见数字签名一节）
- [ ] 线性化（web 优化）未实现
- [ ] 增量更新仅用于追加签名字段修订段（最小字节级解析，非通用解析器；
      读取支持经典 xref 表与 xref 流/对象流混排输入，追加段固定写经典 xref 表）

## 图像

- [ ] JBIG2/JPX（JPEG2000）解码
- [ ] 16 位通道
- [ ] ICC 色彩配置
